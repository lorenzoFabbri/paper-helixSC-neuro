# Script for research question 1 of causal roadmap

rq1_prepare_data <- function(is_fake) {
  if (is_fake == TRUE) {
    # Run analyses using fake datasets
  }
}

rq1_run_analysis <- function() {
  
}
