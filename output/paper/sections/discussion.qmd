List of possible limitations:

* The percentiles of the exposures might differ considerably by cohort. When using `avg_comparisons`, we do not consider this. This might cause cohort-dependent positivity issues (?).
